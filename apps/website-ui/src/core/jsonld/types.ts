export interface TItemListElements {
  item?: string
  name: string
}

export interface TJsonLdOptions {
  /** Adds indentation, white space, and line break characters to JSON-LD output. {@link JSON.stringify} */
  space?: string | number
}
